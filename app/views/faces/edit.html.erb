<% title "Update" %>

<h1>Update staff member</h1>

<% if @face.errors.any? %>
    <h4>The following errors prevented the staff member from being updated:</h4>
    <ul>
      <% @face.errors.full_messages.each do |err| %>
          <li><%= err %></li>
      <% end %>
    </ul>
<% end %>

<%= render 'editform' %>

<p>
  <%= link_to "Back to all staff members", faces_path %>
</p>